<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication1"
        tools:targetApi="31">
        <activity
            android:name=".setting_activity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".ProductDetil"
            android:exported="false" />
        <activity
            android:name=".information"
            android:exported="false" />
        <activity
            android:name=".AddPage"
            android:exported="false"
            android:label="AddPage"
            android:parentActivityName=".AddPage" />
        <activity
            android:name=".searchPage"
            android:exported="false"
            android:label="Searching"/>
        <activity android:name=".search_addpage"
            android:exported="false"
            android:label="Add shop data"/>
        <activity
            android:name=".activity_main"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>